"""groundwork — find, acquire and prepare UK public geodata.

Three pieces, each useful on its own:

  the catalogue   what exists, on which boundary vintage, under what licence
  the conformer   a source table into canonical form (not yet built)
  the checker     a data file against expectations (not yet built)

The catalogue is data, not code: `catalogue.yaml` in the repository root.
Everything here is a small engine over it.
"""

__version__ = "0.1.0.dev0"
